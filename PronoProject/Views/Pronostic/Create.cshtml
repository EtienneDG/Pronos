@model PronoProject.Models.PronosticModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Pronostics</legend>
        <div class="editor-label">
            @Html.LabelFor(model => model.Score_opp_1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Score_opp_1)
            @Html.ValidationMessageFor(model => model.Score_opp_1)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Score_opp_2)
        </div>

        <div class="ui-group-buttons">
            <button type="button" class="btn btn-default btn-lg">@Model.game.Opponent_1</button>
            <div class="or or-lg"></div>
            <button type="button" class="btn btn-default btn-lg">@Model.game.Opponent_2</button>
        </div>
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
