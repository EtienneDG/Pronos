@model PronoProject.Models.Pronostics

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Pronostics</legend>

    <div class="display-label">Score_opp_1</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Score_opp_1)
    </div>

    <div class="display-label">Score_opp_2</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Score_opp_2)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
