@model PronoProject.Models.Games

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Games</legend>

    <div class="display-label">Opponent_1</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Opponent_1)
    </div>

    <div class="display-label">Opponent_2</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Opponent_2)
    </div>

    <div class="display-label">Date</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Date)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
